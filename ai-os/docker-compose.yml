services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: aios
      POSTGRES_PASSWORD: aios
      POSTGRES_DB: aios
    ports:
      - "5433:5432"
    volumes:
      - aios_pgdata:/var/lib/postgresql/data

volumes:
  aios_pgdata:
